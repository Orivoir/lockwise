{% extends "base.html.twig" %}

{% block title %}
    accounts list
{% endblock %}

{% block body %}

    <h1>
        your <b>{{ accounts|length }}</b>
        account{{ accounts|length > 1 ? "s" : "" }} on
    </h1>

    {% include "core/add-account.html.twig" %}

    <section>
        <ul>
            <li>
                <a href="{{path("main.index")}}">
                    favorites
                </a>
            </li>

            <li>
                <a href="{{path("account.warn")}}">
                    warnings
                </a>
            </li>

            <li>
                <a href="{{path("account.error")}}">
                    errors
                </a>
            </li>
        </ul>
    </section>

    {% include "core/list-account.html.twig" %}

{% endblock %}